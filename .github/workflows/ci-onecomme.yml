name: test of install

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13-xlarge # pagefind not working on x64 architecture
    steps:
    - name: Check Out Repo
      uses: actions/checkout@v4
    - name: Tap Repo
      run: brew tap legnoh/etc
